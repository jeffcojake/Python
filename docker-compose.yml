services:
  actual-budget:
    image: ghcr.io/jeffcojake/actual-budget-custom:v1.0
    container_name: actual-budget
    restart: unless-stopped
    ports:
      - "5006:5006"
    volumes:
      - actual-data:/data

volumes:
  actual-data:
